{
  "name": "@bull-trigger/backend",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "npm run migrate && ts-node --esm src/index.ts",
    "build": "tsc -p .",
    "start": "npm run migrate && node dist/index.js",
    "migrate": "ts-node --esm src/db/runMigrate.ts",
    "seed": "ts-node --esm src/db/seed.ts"
  },
  "dependencies": {
    "fastify": "^4.27.2",
    "fastify-env": "^6.0.0",
    "dotenv-safe": "^8.2.0",
    "@crypto-kush/common": "workspace:*",
    "better-sqlite3": "^9.0.0",
    "drizzle-orm": "^0.30.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.1",
    "@types/node": "^20.6.0",
    "drizzle-kit": "^0.19.8"
  }
} 